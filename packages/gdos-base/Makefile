.PHONY: all install
all:

# A metapackage has no payload. Its dependency closure defines the default
# system image, just as alpine-base does for an Alpine installation.
install:
	install -d $(DESTDIR)
